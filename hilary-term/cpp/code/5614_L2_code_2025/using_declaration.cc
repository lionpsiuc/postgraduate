#include <iostream>

int main()
{
    using std::cout; 	// Creates a local name "cout" which is an alias for std::cout

    cout << "Hello World" << std::endl; //std::endl (newline + flushes buffer)
    return 0;
}
